version: v1.0
name: Pipeline 2
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu1804
blocks:
  - name: Deploy
    task:
      jobs:
        - name: 'Deploy '
          commands:
            - echo "deploying using key $API_KEY"
            - 'curl "http://a.single-one.top:8101/"'
      env_vars:
        - name: API_KEY
          value: HELLO_WORLD
